% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orthoptera_species_with_real_sounds.R
\name{orthoptera_species_with_real_sounds}
\alias{orthoptera_species_with_real_sounds}
\title{Get Orthoptera Species with Sound Recordings from iNaturalist}
\usage{
orthoptera_species_with_real_sounds(
  min_recordings = 100,
  taxon_id = 47651,
  taxon_name = NULL,
  place_name = "Australia",
  place_id = NULL,
  quality_grade = "research",
  per_page = 200,
  max_pages = Inf,
  allowed_licenses = tolower(c("cc0", "cc-by", "cc-by-sa", "cc-by-nc", "cc-by-nc-sa"))
)
}
\arguments{
\item{min_recordings}{Numeric. Minimum number of valid recordings required to
include a species in results. Default is 100.}

\item{taxon_id}{Integer. The iNaturalist taxon ID to search within. Default is
47651 (Orthoptera). Ignored if \code{taxon_name} is provided.}

\item{taxon_name}{Character. Optional taxon name to resolve to an ID. If provided,
overrides \code{taxon_id}. Default is NULL.}

\item{place_name}{Character. Name of the place to search within. Default is
"Australia".}

\item{place_id}{Integer. Optional iNaturalist place ID. If NULL, will be resolved
from \code{place_name}. Default is NULL.}

\item{quality_grade}{Character. Quality grade filter for observations. Default is
"research".}

\item{per_page}{Integer. Number of results per API page. Default is 200.}

\item{max_pages}{Numeric. Maximum number of pages to retrieve. Use Inf for all
available pages. Default is Inf.}

\item{allowed_licenses}{Character vector. Lowercase license codes to accept.
Default includes Creative Commons licenses: cc0, cc-by, cc-by-sa, cc-by-nc,
cc-by-nc-sa.}
}
\value{
A data.frame with columns: taxon_id, scientific_name, common_name,
  n_recordings. Sorted by n_recordings in descending order. Returns empty
  data.frame if no species meet criteria.
}
\description{
Queries the iNaturalist API to find species-level taxa with sound recordings
that meet specified criteria. Counts valid sound recordings per species based
on license requirements.
}
\examples{
\dontrun{
# Get Orthoptera species with at least 100 recordings in Australia
orth_aus <- orthoptera_species_with_real_sounds(
  min_recordings = 100,
  place_name = "Australia"
)

# Search by taxon name instead of ID
crickets <- orthoptera_species_with_real_sounds(
  taxon_name = "Gryllidae",
  min_recordings = 50,
  place_name = "United States"
)
}

}
